name: Auto Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.ASTON_HOST }}
          username: ${{ secrets.ASTON_USER }}
          key: ${{ secrets.ASTON_SSH_KEY }}
          port: 22
          script: /home/cs2team17/deploy.sh
